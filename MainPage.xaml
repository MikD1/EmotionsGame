<Page
    x:Class="EmotionsGame.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <CaptureElement x:Name="CaptureView"/>
            <Canvas x:Name="CanvasView"/>

            <TextBlock x:Name="Player1ScoreView" Text="0" HorizontalAlignment="Left" VerticalAlignment="Bottom" FontSize="30"
                       Foreground="Yellow" FontWeight="Bold"/>
            <TextBlock x:Name="Player2ScoreView" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontSize="30"
                       Foreground="Yellow" Text="0" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Page>
